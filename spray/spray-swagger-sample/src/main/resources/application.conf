akka {
  loggers = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug
  actor {
    debug {
      receive = on
      lifecycle = on
    }
  }
}

spray-swagger-sample {
  foo = bar
}

http {
  interface="0.0.0.0"
  port=8080
}
